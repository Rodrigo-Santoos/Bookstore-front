
1//tem que ter um modelo para apresentar os dados
//esse e o modelo de categorias, e as variaveis tem que ser identica ao do backend
export interface Categoria{
    id?: string;
    nome: string;
    descricao: string;
}